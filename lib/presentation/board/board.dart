import 'package:flutter/material.dart';
import 'package:todo_app/presentation/board/board_widget.dart';

class Board extends StatelessWidget {
  const Board({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return BoardWidget(
      
    );
  }
}